{
  "name": "saas-360-main",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "^1.11.2"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}